service: microproject

frameworkVersion: ">=1.7.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs10.x
  profile: serverless

functions:
  hello:
    handler: handler.greet

package:
  exclude:
    - Resources/**
    - cli_commands
